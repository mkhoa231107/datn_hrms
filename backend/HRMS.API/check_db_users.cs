using Microsoft.EntityFrameworkCore;
using HRMS.Infrastructure.Data;
using System;
using System.Linq;

// This script will be run via a temporary console app or similar
// But simpler: just use PowerShell to query the SQL Server if we know the connection string
